---
title: "Gestione Eccezioni"
draft: false
lastUpdateDate: "2025-11-07"
subject: "Python"
category: "Informatica"
tags: ["python", "programmazione", "try", "except", "eccezioni", "medio"]
author: "Dennis Turco"
videoLesson: false
---

## 1. Introduzione

Durante l'esecuzione di un programma Python, possono verificarsi errori.
Alcuni di questi sono **errori di sintassi**, che impediscono l'esecuzione del programma; altri sono **eccezioni**, cioè errori che si verificano durante l'esecuzione (runtime errors).

Esempi di eccezioni comuni:
- `ValueError`: si verifica quando un valore non è del o previsto.
- `ZeroDivisionError`: divisione per zero.
- `FileNotFoundError`: apertura di un file inesistente.
- `TypeError`: operazioni tra i non compatibili (es. sommare stringa e intero).
- `IndexError`: accesso a un indice di lista inesistente.

Per gestire in modo controllato le eccezioni, Python mette a disposizione il costrutto:

**try → except → else → finally**

![try-except-finally](https://www.boardinfinity.com/blog/content/images/2023/02/Try--except--else-and-finally-python-1.png)

Non tutti i blocchi sono obbligatori, ma devono seguire quest'ordine.

## 2. Blocco Try

Il blocco `try` contiene il codice che potrebbe **generare un'eccezione**.
Se durante l'esecuzione di quel codice si verifica un'eccezione non gestita, il controllo viene passato ai blocchi `except` successivi.

Esempio:

```python
try:
    x = int(input("Inserisci un numero: "))
    print("Hai inserito:", x)
```

Se l'utente inserisce qualcosa che non può essere convertito in intero (es. "abc"), viene sollevata un'eccezione `ValueError`.

<Aside variant="info">
Il blocco `try` non serve a prevenire gli errori, ma a gestirli **in modo ordinato** quando si verificano.
</Aside>

## 3. Blocco except

`except` serve per **intercettare e gestire** le eccezioni.
Può essere generico o specifico.

![try-except](https://embeddedinventor.com/wp-content/uploads/2023/04/1-1.png)

1. Esempio base (cattura qualsiasi eccezione)

    ```python
    try:
        x = int(input("Inserisci un numero: "))
    except:
        print("Si è verificato un errore!")
    ```

    <Aside variant="warning">
    Questo approccio è poco raccomandato perché nasconde informazioni utili al debug.
    </Aside>

2. Esempio con o specifico

    ```python
    try:
        x = int(input("Inserisci un numero: "))
    except ValueError:
        print("Devi inserire un numero valido!")
    ```

    Intercetta solo `ValueError`, lasciando passare eventuali altri errori (es. `KeyboardInterrupt`).

3. Più `except` per i diversi

    ```py
    try:
        x = int(input("Inserisci un numero: "))
        risultato = 10 / x
    except ValueError:
        print("Non hai inserito un numero!")
    except ZeroDivisionError:
        print("Non puoi dividere per zero!")
    ```

4. Gestire più eccezioni in una sola clausola

    ```py
    try:
        x = int(input("Inserisci un numero: "))
        risultato = 10 / x
    except (ValueError, ZeroDivisionError):
        print("Errore: input non valido o divisione per zero!")
    ```

## 4. Blocco else

Il blocco `else` **viene eseguito solo se non si è verificata alcuna eccezione** nel `try`.
Serve per separare il codice di "successo" da quello di "errore".

![else](https://embeddedinventor.com/wp-content/uploads/2023/04/3-1.png.webp)

Esempio:

```py
try:
    x = int(input("Inserisci un numero: "))
except ValueError:
    print("Errore: non è un numero valido.")
else:
    print("Hai inserito correttamente il numero:", x)
```

<Aside variant="info">
Usare `else` migliora la leggibilità e chiarisce l'intento del programmatore: "se tutto è andato bene, esegui questo blocco".
</Aside>

## 5. Blocco finally

`finally` **viene sempre eseguito**, indipendentemente dal fatto che sia avvenuta o meno un'eccezione.
È utile per **rilasciare risorse**, come file o connessioni a database.

![finally](https://embeddedinventor.com/wp-content/uploads/2023/04/2-2.png.webp)

Esempio

```py
try:
    file = open("dati.txt", "r")
    contenuto = file.read()
except FileNotFoundError:
    print("File non trovato!")
else:
    print("File letto con successo.")
finally:
    file.close()
    print("File chiuso.")
```

<Aside variant="info">
Anche se si verifica un'eccezione, il `finally` viene eseguito (a meno che il processo non venga terminato forzatamente).
</Aside>

## 6. Schema Generale

```py
try:
    # codice che potrebbe generare un errore
except oErrore1:
    # gestisce il primo o di errore
except oErrore2:
    # gestisce il secondo o di errore
else:
    # eseguito se non ci sono errori
finally:
    # eseguito sempre
```

<Aside variant="info">
Note:
- Possibile avere zero o più blocchi `except`.
- `else` è opzionale e va dopo tutti gli `except`.
- `finally` è opzionale e, se presente, va per **ultimo**.
</Aside>

## 7. Rilancio delle Eccezioni (raise)

Possibile **rilanciare** l'eccezione dopo averla parzialmente gestita, per farla propagare più in alto nello stack:

```py
try:
    x = int(input("Inserisci un numero: "))
except ValueError:
    print("Errore catturato, ma lo rilancio...")
    raise
```

<Aside variant="tip">
`raise` senza argomenti rilancia l'ultima eccezione catturata. È utile per registrare un errore o effettuare operazioni di pulizia, senza sopprimere l'errore originale.
</Aside>

## 8. Eccezioni personalizzate

Possibile creare eccezioni custom definendo una classe che eredita da `Exception`:

```py
class NumeroNegativoError(Exception):
    pass

try:
    n = int(input("Inserisci un numero positivo: "))
    if n < 0:
        raise NumeroNegativoError("Hai inserito un numero negativo!")
except NumeroNegativoError as e:
    print("Errore personalizzato:", e)
```

Le eccezioni personalizzate migliorano la chiarezza e rendono il codice più robusto.

<Aside variant="info">
Possibile anche aggiungere logica personalizzata alla classe, ad esempio campi aggiuntivi o metodi per formattare il messaggio di errore.
</Aside>

## 9. Buone pratiche

1. Evitare except generici, sempre meglio specificare l'eccezione attesa
2. Loggare le eccezioni importanto con `logging`
3. Rilanciare (raise) quando non si è al livello giusto per decidere come gestire l'errore.
4. Definire eccezioni custom per condizioni di errore logiche dell'applicazione.

## 10. Quiz a risposta multipla

import Quiz from '../../../components/Quiz.jsx';

<Quiz client:load questions={[
  {
    question: "1) Qual è la differenza principale tra un errore di sintassi e un'eccezione?",
    answers: {
      a: "L'errore di sintassi viene rilevato a runtime, l'eccezione a compilazione",
      b: "L'errore di sintassi impedisce l'esecuzione del programma, mentre un'eccezione può essere gestita",
      c: "L'eccezione è più grave di un errore di sintassi",
      d: "Non c'è alcuna differenza"
    },
    correctAnswer: "b"
  },
  {
    question: "2) Cosa accade se si verifica un'eccezione all'interno di un blocco try senza un blocco except corrispondente?",
    answers: {
      a: "L'errore viene ignorato e il programma continua",
      b: "Il programma si blocca mostrando un messaggio di errore",
      c: "Viene automaticamente eseguito un blocco finally",
      d: "Python tenta di correggere l'errore"
    },
    correctAnswer: "b"
  },
  {
    question: "3) Qual è la sintassi corretta per catturare un'eccezione specifica in Python?",
    answers: {
      a: "try: ... except(ValueError): ...",
      b: "try { ... } catch(ValueError): ...",
      c: "try: ... except ValueError: ...",
      d: "catch ValueError: ..."
    },
    correctAnswer: "c"
  },
  {
    question: "4) Qual è lo scopo del blocco else in una struttura try-except?",
    answers: {
      a: "Viene eseguito solo se si verifica un'eccezione",
      b: "Serve per chiudere risorse come file o connessioni",
      c: "Viene eseguito solo se nessuna eccezione è stata sollevata",
      d: "Serve per rilanciare eccezioni"
    },
    correctAnswer: "c"
  },
  {
    question: "5) Quale delle seguenti affermazioni sul blocco finally è vera?",
    answers: {
      a: "Viene eseguito solo se si verifica un'eccezione",
      b: "Viene eseguito sempre, anche se c'è un'eccezione",
      c: "Serve per catturare eccezioni multiple",
      d: "Non può essere usato insieme al blocco else"
    },
    correctAnswer: "b"
  },
  {
    question: "6) Come si possono gestire più tipi di eccezioni in un singolo blocco except?",
    answers: {
      a: "except ValueError, ZeroDivisionError:",
      b: "except (ValueError, ZeroDivisionError):",
      c: "except [ValueError, ZeroDivisionError]:",
      d: "except {ValueError, ZeroDivisionError}:"
    },
    correctAnswer: "b"
  },
  {
    question: "7) Cosa fa l'istruzione 'raise' in Python?",
    answers: {
      a: "Termina il programma immediatamente",
      b: "Ignora l'eccezione corrente",
      c: "Solleva un'eccezione in modo esplicito",
      d: "Crea un nuovo blocco try"
    },
    correctAnswer: "c"
  },
  {
    question: "8) Quando si usa 'raise' senza argomenti all'interno di un blocco except?",
    answers: {
      a: "Per rilanciare la stessa eccezione catturata",
      b: "Per terminare il blocco except",
      c: "Per creare una nuova eccezione personalizzata",
      d: "Per saltare il blocco finally"
    },
    correctAnswer: "a"
  },
  {
    question: "9) Come si definisce correttamente un'eccezione personalizzata in Python?",
    answers: {
      a: "class ErrorePersonalizzato:",
      b: "class ErrorePersonalizzato(Exception): pass",
      c: "def ErrorePersonalizzato(Exception): pass",
      d: "raise ErrorePersonalizzato()"
    },
    correctAnswer: "b"
  },
  {
    question: "10) Qual è una buona pratica nella gestione delle eccezioni?",
    answers: {
      a: "Usare except senza specificare l'errore",
      b: "Evitare di usare finally",
      c: "Gestire solo le eccezioni attese e specifiche",
      d: "Ignorare gli errori per migliorare la performance"
    },
    correctAnswer: "c"
  }
]} />


## 11. Esercizi

### 11.1 Esercizio - Conversione Sicura

Scrivere un programma che:
1. chiede un numero intero all'utente.
2. stampa il quadrato del numero.
3. gestisce gli errori in caso di input non valido.

Suggerimento: usare try-except-else.

### 11.2 Esercizio - Divisione Sicura

Scrivere un programma che:
1. chiede due numeri (dividendo e divisore).
2. calcola il risultato della divisione.
3. gestisce i casi di input non numerico e divisione per zero.
4. stampa sempre un messaggio finale "Operazione terminata".

### 11.3 Esercizio -Eccezione Personalizzata

Crea una classe di eccezione "EtaNonValidaError" e scrivere un programma che:

1. chiede l'età all'utente.
2. solleva l'eccezione se l'età è minore di 0 o maggiore di 120.
3. gestisce l'errore con un messaggio personalizzato.