---
title: "Metodi avanzati e gestione degli attributi"
order: 14
draft: false
lastUpdateDate: "2025-11-07"
subject: "Python"
category: "Informatica"
tags: ["python", "programmazione", "oop", "classi", "metodi", "property"]
author: "Dennis Turco"
videoLesson: false
---

## 1. Introduzione

In questa lezione approfondiamo alcuni aspetti più avanzati della programmazione orientata agli oggetti (OOP) in Python.

Dopo aver visto come creare classi, oggetti e metodi di base, esploreremo:

- Costruttori alternativi, per creare oggetti da formati diversi (come stringhe);
- Tipologie di metodi, per capire la differenza tra metodi di istanza, di classe e statici;
- Proprietà (`@property`), per controllare l'accesso e la modifica degli attributi.

Questi concetti rendono il codice più flessibile, leggibile e Pythonic.

## 2. Costruttori alternativi (`@classmethod`)

Oltre al costruttore classico `__init__`, una classe può avere costruttori alternativi,
utili quando si vogliono creare oggetti da sorgenti di dati diverse.

Per farlo si usa il decoratore `@classmethod`.

```python
class Persona:
    def __init__(self, nome, eta):
        self.nome = nome
        self.eta = eta

    @classmethod
    def da_stringa(cls, stringa):
        nome, eta = stringa.split(",")
        return cls(nome, int(eta))

p2 = Persona.da_stringa("Marco,40")
```

<Aside variant="info">
`cls` rappresenta la **classe stessa**, e non l'istanza.
È utile quando si vogliono creare metodi che generano oggetti in modo personalizzato.
</Aside>

## 3. Tipologie di Metodi

In Python esistono tre tipi principali di metodi all'interno delle classi:

| Tipo              | Decoratore      | Primo parametro | Accesso a...                    |
| ----------------- | --------------- | --------------- | ------------------------------- |
| Metodo di istanza | —               | `self`          | Attributi e metodi dell'oggetto |
| Metodo di classe  | `@classmethod`  | `cls`           | Attributi e metodi della classe |
| Metodo statico    | `@staticmethod` | —               | Nessuno, è indipendente         |


Esempio:

```python
class Calcolatrice:
    @classmethod
    def info(cls):
        print("Classe per calcoli matematici.")

    @staticmethod
    def somma(a, b):
        return a + b

Calcolatrice.info()
print(Calcolatrice.somma(5, 3))
```

<Aside variant="tip">
I metodi statici sono utili per funzioni “utility” che hanno senso all'interno di una classe, ma **non dipendono** dallo stato di essa.
</Aside>

## 4. Proprietà (`@property`)

Le **property** permettono di gestire l'accesso agli attributi in modo controllato, mantenendo una sintassi semplice come se si accedesse a un campo pubblico.

Sono particolarmente utili per **validare** o **filtrare i dati** durante la lettura o la scrittura.

```python
class Persona:
    def __init__(self, nome, eta):
        self._nome = nome
        self._eta = eta

    @property
    def eta(self):
        return self._eta

    @eta.setter
    def eta(self, valore):
        if valore < 0:
            print("L'età non può essere negativa!")
        else:
            self._eta = valore
```

Uso:

```python
p = Persona("Luca", 25)
print(p.eta)   # usa il getter
p.eta = -3     # usa il setter → messaggio di errore
```

<Aside variant="info">
Le property rendono il codice **più robusto** e permettono di introdurre logica di controllo **senza cambiare** la sintassi di accesso agli attributi.
</Aside>

## 5. Esercizi

### 5.1 Esercizio - Costruttore alternativo

Creare una classe `Libro` con:

- attributi `titolo`, `autore` e `prezzo`;
- un costruttore alternativo `da_stringa("titolo;autore;prezzo")`.

### 5.2 Esercizio - Property

Creare una classe `Studente` con un attributo `voto` e una property che:

- permette di leggere il voto;
- impedisce di impostare valori minori di 0 o maggiori di 10.

## 6. Quiz a risposta multipla

import Quiz from '../../../components/Quiz.jsx';

<Quiz client:load questions={[
  {
    question: "1) Qual è la funzione principale di un metodo con decoratore @classmethod?",
    answers: {
      a: "Gestire gli attributi di un'istanza",
      b: "Creare o gestire la classe stessa invece di un'istanza",
      c: "Eseguire operazioni indipendenti dalla classe",
      d: "Controllare la scrittura di un attributo"
    },
    correctAnswer: "b"
  },
  {
    question: "2) Qual è il primo parametro che riceve un metodo di classe?",
    answers: {
      a: "self",
      b: "this",
      c: "cls",
      d: "obj"
    },
    correctAnswer: "c"
  },
  {
    question: "3) In quale caso è utile un costruttore alternativo?",
    answers: {
      a: "Quando si vogliono creare oggetti da formati diversi, come stringhe o file",
      b: "Solo quando si devono modificare attributi privati",
      c: "Quando serve rendere gli attributi statici",
      d: "Per sostituire completamente __init__"
    },
    correctAnswer: "a"
  },
  {
    question: "4) A cosa serve il decoratore @staticmethod?",
    answers: {
      a: "A creare un metodo che non dipende né dalla classe né dall'istanza",
      b: "A rendere il metodo più veloce",
      c: "A proteggere gli attributi della classe",
      d: "A gestire la lettura di un attributo"
    },
    correctAnswer: "a"
  },
  {
    question: "5) Qual è la differenza tra un metodo statico e uno di classe?",
    answers: {
      a: "Il metodo statico riceve self, quello di classe riceve cls",
      b: "Il metodo di classe riceve la classe come primo parametro, quello statico nessuno",
      c: "Sono equivalenti in Python",
      d: "Il metodo statico può accedere agli attributi della classe"
    },
    correctAnswer: "b"
  },
  {
    question: "6) Cosa consente di fare il decoratore @property?",
    answers: {
      a: "Creare un attributo privato",
      b: "Trasformare un metodo in un attributo leggibile come una proprietà",
      c: "Bloccare la modifica di un attributo",
      d: "Creare automaticamente un costruttore alternativo"
    },
    correctAnswer: "b"
  },
  {
    question: "7) Cosa accade se in una property setter si tenta di assegnare un valore non valido?",
    answers: {
      a: "Python genera sempre un errore di tipo",
      b: "Il valore viene comunque assegnato",
      c: "È possibile gestirlo con logica personalizzata (es. messaggio di errore o blocco)",
      d: "L'attributo viene eliminato"
    },
    correctAnswer: "c"
  },
  {
    question: "8) Qual è il vantaggio principale nell'usare @property?",
    answers: {
      a: "Permette di rinominare attributi senza modificare il codice che li usa",
      b: "Aumenta la velocità del programma",
      c: "Evita l'uso di metodi in una classe",
      d: "Serve solo per nascondere attributi privati"
    },
    correctAnswer: "a"
  },
  {
    question: "9) Quale dei seguenti è un esempio corretto di costruttore alternativo?",
    answers: {
      a: "def __init__(self): pass",
      b: "@staticmethod def crea(): return Persona()",
      c: "@classmethod def da_stringa(cls, s): return cls(*s.split(','))",
      d: "def factory(): return new Persona()"
    },
    correctAnswer: "c"
  },
  {
    question: "10) In quale caso si userebbe un metodo statico?",
    answers: {
      a: "Quando serve una funzione di utilità che non usa né self né cls",
      b: "Quando serve modificare gli attributi della classe",
      c: "Quando si vuole accedere a variabili d'istanza",
      d: "Solo per gestire property e setter"
    },
    correctAnswer: "a"
  }
]} />
