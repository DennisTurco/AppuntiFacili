---
import { supabase } from "@/lib/supabaseClient";
import { onMount } from "solid-js";
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/container.astro";

onMount(async () => {
  // Questo sincronizza la sessione lato client
  const { data: { session }, error } = await supabase.auth.getSession();
  console.log("Sessione attiva:", session);
});
---

<Layout title="Conferma">
  <Container>
<div class="container mx-auto px-6 py-12">
  <h1 class="text-2xl font-bold text-center">Benvenuto!</h1>
  <p class="text-center mt-4">La tua email è stata confermata. Ora puoi accedere.</p>
</div>

<div class="text-center mt-6">
  <a
    href="/ImparareFacile/login"
    class="bg-blue-600 text-white px-4 py-2 rounded-lg shadow hover:bg-blue-700"
  >
    Vai al Login
  </a>
</div>
</Container>
</Layout>